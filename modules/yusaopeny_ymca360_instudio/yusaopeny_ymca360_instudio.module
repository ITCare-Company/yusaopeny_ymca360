<?php

/**
 * @file
 * YMCA360 Integration: in-studio and in-person schedules routines.
 */

/**
 * Implements hook_cron().
 */
function yusaopeny_ymca360_instudio_cron() {
  $config = \Drupal::config('yusaopeny_ymca360.settings');
  if (!$config->get('cron.enable_cron')) {
    return;
  }
  \Drupal::service('ymca_sync.syncer')
    ->run('yusaopeny_ymca360_instudio.syncer', 'proceed');
}
